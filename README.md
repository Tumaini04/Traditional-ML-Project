# Traditiona-ML-Project
Built a machine learning project to classify breast cancer tumours as benign or malignant using supervised and unsupervised learning. Applied preprocessing, median imputation, SMOTE, and scaling. Trained a Random Forest classifier and used DBSCAN clustering with PCA, evaluating performance through accuracy, recall, precision, and cross-validation.
